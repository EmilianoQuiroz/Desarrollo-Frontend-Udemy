/*
EJERCICIO:
Pedirle al usuario que ingrese dos numero por consola y posteriormente indicarle si este es par o impar
*/

let numero = 23;//Inicializamos la variable con el numero que querramos

if (numero %= 0)//Si el resto de la variable es 0 entonces es par y ejecutamos el siguiente console
{
    console.log("Es par ");
}
else// Si el resto de la variable no es 0 entonces es par y ejecutamos el siguiente console
{
    console.log("No es par ");
}
